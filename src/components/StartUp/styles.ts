import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    mediumText: {
        fontSize: '16px',
        lineHeight: '1.5',
        whiteSpace: 'pre-line'
    },
    alignCenter: {
        textAlign: 'center'
    }
}));